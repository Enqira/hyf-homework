const todo = ["fishing", "singing"]
const names = []
function getReply(command) {
    for (i=0; i<command.length; i++){   //we loop througth the command
        if (command[i] == "name"){     //if the command contains the world name
            nameFunc(command)          //we run nameFunc function
        }
    }
}





nameFunc(command){

}